class AbstractOutput {
    size;
    maxColumnWidth;
    maxColumnHeight;
    constructor(size, maxColumnWidth, maxColumnHeight) {
        this.size = size;
        this.maxColumnWidth = maxColumnWidth;
        this.maxColumnHeight = maxColumnHeight;
    }
}
export default AbstractOutput;
//# sourceMappingURL=AbstractOutput.js.map